<UserControl x:Class="gyro1.NxtPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel >
            <TextBlock Text="Touch1" />
            <Rectangle Fill="{Binding Touch1Brush}" Width="40" Height="40" Stroke="Black" StrokeThickness="1.0" />
            <Separator />
            <Slider Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Stretch"  Maximum="8000" Value="{Binding Battery}" />
        </StackPanel>
        <xctk:PropertyGrid SelectedObject="{Binding}" Margin="4" Grid.Column="1" Grid.RowSpan="2" />
    </Grid>
</UserControl>